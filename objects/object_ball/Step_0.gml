// Here's an optional line of code to try:
// it gives the ball a kind of fake spin
// (It doesn't really adhere to the laws of physics).
// uncomment the code and try it out.

image_angle = image_angle + (hspeed - vspeed);
